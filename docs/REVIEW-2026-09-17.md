# Weekly tools review — 2026-09-17

Reviewed the working-tree changes and recent commits through `ce40d36`,
including the advanced usage export, scoring audit, play logs, and browser
regressions. The new tools remain descriptive research inputs; no projection
model or ESPN transaction behavior was changed.

## Issues fixed

- Club reports dropped Monday and Tuesday practice cells for Thursday games.
  Preserve all seven possible day columns and the source's day order.
- Filtered club exports could overwrite the league snapshot. Give them
  separate filenames; record empty pages and missing teams in the manifest.
  Reject historical seasons because the club URLs contain no season.
- The rankings CLI could label live consensus as an arbitrary week. Require
  the current regular-season week, its schedule, and matching D/ST pairings
  before writing. Handle bye mismatches without a `KeyError`, and expose
  position-level fetch failures.
- Bluesky fetch failures disappeared during date filtering. Emit coverage
  warnings and describe results as a bounded sample. Load Bluesky credentials
  supplied solely through environment variables.
- Team context defaulted to the newest represented week even if only its
  Thursday game was available. Require all scheduled teams for the default;
  handle empty week ranges and missing rates without crashing.
- nflfastR flags sacks as pass attempts. Exclude them from the team-context
  attempt count while retaining them in dropback efficiency.
- Player lookup crashed with older exports lacking snap columns, or rows
  with a missing snap percentage. Skip unavailable snap fields safely.
- Offline weekly builds still requested the ESPN scoreboard. Skip that
  request when refresh is disabled.
- Corrected the injury-research README's claim that class imbalance explains
  AUC; AUC does not establish recall at a chosen decision threshold.

## Validation

- Full `npm test` passed using `.venv-league-sim`: 11 JavaScript unit tests,
  6 local-server tests, 4 rank-update tests, and all 18 browser E2E tests.
- All 58 weekly tests passed, including the real MCP stdio transport,
  source failures, wrong-week rejection, practice-day parsing, snap coverage,
  missing values, and weighted pooling. The two temporal scoring-audit tests
  also passed.
- Full weekly build completed in a temporary output directory with network
  access blocked: 14 outputs, 240 skill rows, and 32 teams with lines.
- Live MCP calls passed for status, team context (team and league views),
  consensus rankings, player lookup with snaps, and the ESPN roster read.
- Live club reads covered all 32 hosts; transactions and depth charts also
  returned data. Snap export produced 309 player-weeks; team context produced
  32 team-weeks. Output hashes and CSV round trips were checked.
- Live Bluesky feed filtering and account activity checks returned data.
  Authenticated keyword search was tested with mocked credentials/session
  exchange, not a live account.

## Source limitations observed

The club snapshot contains 181 rows covering 28 teams. LA, NO, NYG, and NYJ
had no parsed report; the manifest lists them as missing, not healthy.
One consensus fetch received HTTP 503 for WR and returned 206 rows from
the remaining positions; the final verification fetch recovered all 266 rows.
Feed warnings and manifests expose partial coverage.
The independent position files have no embedded week IDs; D/ST matchup
validation does not prove that every position updated simultaneously.

ESPN roster writes were not exercised. No roster transaction was requested.
Third-party consensus files remain ignored by git.
